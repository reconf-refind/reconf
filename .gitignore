# ignore everything by default
*

# allow JSON files (including jsonlines)
!*.json

# allow JS and TS files
!*.js
!*.ts

# allow .reconf files (and anything starting with .reconf)
!.reconf*
# allow RC config files
!*rc
# allow ignore files
!.gitignore
!.npmignore

# allow directories (so git can track allowed files inside)
!*/

# explicitly ignore node_modules and other common junk
node_modules/
dist/
bin/
build/
